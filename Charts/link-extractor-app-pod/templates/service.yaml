apiVersion: v1
kind: Service
metadata:
  name: {{ include "link-extractor-app-pod.fullname" . }}
  labels:
    {{- include "link-extractor-app-pod.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "link-extractor-app-pod.selectorLabels" . | nindent 4 }}
